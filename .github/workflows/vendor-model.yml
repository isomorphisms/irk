name: Vendor meaning model

on:
  push:
    paths:
      - model/MODEL.lock
  workflow_dispatch:

permissions:
  contents: write

jobs:
  vendor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Download and verify the pinned model
        run: sh model/fetch.sh
      - name: Commit model files when they changed
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add model/config.json model/model.safetensors model/tokenizer.json
          git diff --cached --quiet && exit 0
          git commit -m "Vendor the pinned meaning model"
          git push

